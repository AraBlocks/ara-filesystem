const debug = require('debug')('ara-filesystem:test:aid')
const test = require('ava')
const aid = require('../aid')

test("create(publicKey)", async (t) => {

})

test("archive(identity, opts)", async (t) => {

})

test("resolve(did, opts = {})", async (t) => {
  
})
